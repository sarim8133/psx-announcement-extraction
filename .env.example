# Copy this file to `.env` and paste your real key. The real `.env` is gitignored
# and must NEVER be committed — a leaked key in public git history is permanent.
GOOGLE_API_KEY=your_gemini_api_key_here
